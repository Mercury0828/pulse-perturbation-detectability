# PDET Phase-0 numerics — pinned environment (frozen 2026-06-28)
# Python 3.12.10
numpy
scipy
qutip==5.3.0
qiskit==1.3.0
qiskit-dynamics==0.6.0
stim
matplotlib
# Core propagator: scipy.linalg.expm on piecewise-constant Hamiltonian (exact for 1-2 qubit DRAG/CR).
# QuTiP used as an independent cross-check (DD-0.3). Qiskit Dynamics available for pulse-model parity checks.
