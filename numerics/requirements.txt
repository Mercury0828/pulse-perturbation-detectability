# PDET numerics -- pinned to the environment that produced the submitted results.
# Verified 2026-08-21 by importing each package and reading __version__; the
# previous file claimed Python 3.12.10 / qutip 5.3.0, which is not what the
# results were generated with.
#
# Python 3.11.4
numpy==1.26.4
scipy==1.16.1
qutip==5.1.1
matplotlib==3.8.4
stim==1.16.0
#
# stim is REQUIRED, not optional: the operator-spreading boundary of the
# scalability section is computed with it. The modules now fail on a missing
# import rather than recording {"error": ...} and rendering an empty panel.
#
# Core propagator: scipy.linalg.expm on a piecewise-constant Hamiltonian (exact
# for the 1-2 qubit DRAG/CR models). QuTiP supplies the Lindblad propagation for
# the open-system response.
#
# The hardware/ scripts have their own dependencies (qiskit, qiskit-ibm-runtime,
# qiskit-aer) and are not needed to reproduce any simulation result.
